#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v;
    int data;
    while (data!=-1)
    {
        cin>>data;
        if(data!=-1){
            v.push_back(data);
        }
    }
    
    
    return 0;
}